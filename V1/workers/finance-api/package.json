{
  "name": "finance-api-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for Finance — Plaid Link, incremental transaction sync into Firestore, and App-Lock session tokens.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
