{
  "name": "a1-taskhub",
  "private": true,
  "description": "Test tooling for the single-file apps. They have no build step and ship as-is. The Shield desktop agent under desktop/shield is the one exception and builds with cargo.",
  "scripts": {
    "test": "node tests/syntax-check.js && node tests/sync-guard.test.js && node tests/agent-contract.test.js && node tests/emergency-state.test.js && node tests/write-breaker.test.js && node tests/notif-delivery.test.js"
  },
  "devDependencies": {
    "@babel/parser": "^7.25.0"
  }
}
